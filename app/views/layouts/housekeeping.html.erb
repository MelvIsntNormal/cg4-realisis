<!DOCTYPE html>
<html>
	<head>
  		<title><%= yield(:title) %> | Housekeeping | Realisis</title>
  		<%= stylesheet_link_tag    "application/application", media: "all", "data-turbolinks-track" => true %>
  		<%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  		<%= render 'shared/shim' %>
  		<%= csrf_meta_tags %>
	</head>
	<body>
		<%= render 'shared/hk/header' %>
		<div id="main">
			<div class="buffer">
				<%= yield(:pagetitle) %>
			</div>
			<%= render 'shared/flash' %>
    		<%= yield %>
    	</div>
	</body>
</html>
